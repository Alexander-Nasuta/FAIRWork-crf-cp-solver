# this file was automatically generated by Turbo Setup (v ToDo)


[project]
name = "FAIRWork-crf-cp-solver"
version = "0.0.1"
description = ""
readme = "README.md"
authors = [{ name = "Alexander Nasuta", email = "alexander.nasuta@wzl-iqs.rwth-aachen.de" }]
requires-python = ">=3.11"
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
]
license = { file = "LICENSE" }
dependencies = [
    "numpy",
    "ortools",
    "jsp-vis",
    "rich",
]
[project.optional-dependencies]
dev = [
    "pip-tools",
    "pytest",
    "pytest-cov",
    "tox",
    "sphinx",
    "furo",
    "twine",
]
[build-system]
requires = [
    "setuptools>=65.5.0",
    "wheel",
]
build-backend = "setuptools.build_meta"
[tool.pytest.ini_options]
addopts = "--cov=order_scheduling --cov=worker_allocation -p no:warnings"
testpaths = [
    "tests"
]
